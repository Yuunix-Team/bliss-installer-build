#!/bin/bash

# This is the testsuite of gearlock

# functions to be tested: abswitch, gqueue, chroot setup, iso update, mkinitfs and hooks, pipe setup, boot injection, system update, install flashable zip, install gxp, first integration script, pre/post install hooks, file system installability

# requirements: qemu with at least 1 iso image for each android distro, a magisk module, a non-magisk module flashable zip, a gxp package/extension
# test on bliss os and any android >= 10
